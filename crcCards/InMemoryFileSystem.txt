Class name: InMemoryFileSystem
Parent class:

Responsibilities:
* structure, organization and hierarchy of files and directories
* manages the current directory
* manages retrieval, creation and storage of files and directories

Collaborators:
* File
* Directory
